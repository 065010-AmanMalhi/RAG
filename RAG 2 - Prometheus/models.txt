# ============================================================
# models.txt — Model Configuration Reference
# Agentic RAG System — Sample Instance 1
# ============================================================
# This file is parameterized for multi-instance runs.
# Each instance may define a different LLM + Embedding Model
# combination. The RAG system reads this file at runtime.
# ============================================================


# ------------------------------------------------------------
# SUBSYSTEM 1 — Small Language Model (LLM)
# ------------------------------------------------------------

LLM_NAME=lukaspetrik/gemma3-tools:1b
LLM_PROVIDER=ollama
LLM_SIZE=581MB
LLM_CONTEXT_WINDOW=128K
LLM_PULL_COMMAND=ollama pull lukaspetrik/gemma3-tools:1b

# Notes:
# - Quantized variant of Meta Llama 3.2 1B (Q2_K quantization)
# - Fits within the <1GB model size constraint
# - Optimized for instruction-following and agentic retrieval tasks
# - Supports tool calling natively


# ------------------------------------------------------------
# SUBSYSTEM 1 — Embedding Model
# ------------------------------------------------------------

EMBEDDING_MODEL_NAME= jina/jina-embeddings-v2-small-en
EMBEDDING_MODEL_PROVIDER=ollama
EMBEDDING_MODEL_SIZE=49MB
EMBEDDING_MODEL_CONTEXT_WINDOW=4K
EMBEDDING_MODEL_PULL_COMMAND=ollama pull  jina/jina-embeddings-v2-small-en

# Notes:
# - Extremely lightweight embedding model (49MB)
# - Based on mixedbread-ai/mxbai-embed-xsmall-v1 (HuggingFace)
# - Text input only
# - Suitable for semantic similarity and vector retrieval tasks


# ------------------------------------------------------------
# SUBSYSTEM 2 — Judge LLM (Fixed across all instances)
# ------------------------------------------------------------

JUDGE_LLM=llama3.2
JUDGE_LLM_PROVIDER=ollama

# Notes:
# - Judge LLM is not parameterized — llama3.2 is used
#   across all evaluation instances regardless of configuration


# ------------------------------------------------------------
# EVALUATION FRAMEWORK
# ------------------------------------------------------------

# Uncomment exactly ONE framework per instance run:

# EVALUATION_FRAMEWORK=deepeval-llamaindex
# EVALUATION_FRAMEWORK=geval-llamaindex
# EVALUATION_FRAMEWORK=prometheus
EVALUATION_FRAMEWORK=ragas-llamaindex

# Notes:
# - deepeval-llamaindex  : DeepEval via LlamaIndex integration
# - geval-llamaindex     : G-Eval via LlamaIndex integration
# - prometheus           : Prometheus evaluation framework
# - ragas-llamaindex     : RAGAs via LlamaIndex (default for this instance)


# ------------------------------------------------------------
# DATABASE CONFIGURATION
# ------------------------------------------------------------

DB_HOST=localhost
DB_PORT=5432
DB_NAME=amandb1
DB_ROLE=amandb
DB_USER=amandb
DB_VECTOR_EXTENSION=pgvector

# ------------------------------------------------------------
# FILE PATHS
# ------------------------------------------------------------

DATASET_PATH=<dataset_path>
QUESTIONS_PATH=<questions_path>
ANSWERS_PATH=<answers_path>

# ------------------------------------------------------------
# OUTPUT
# ------------------------------------------------------------

OUTPUT_FILE=output_rag.json

# ============================================================
# END OF CONFIGURATION
# ============================================================
